# frozen_string_literal: true

source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins

# to use the Jekyll native, uncomment the line below
# gem "jekyll", ENV["JEKYLL_VERSION"] if ENV["JEKYLL_VERSION"]
